{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "serve:svg-clock": "lerna run --scope svg-clock start --stream",
    "build:svg-clock": "lerna run --scope svg-clock build:lib --stream"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
